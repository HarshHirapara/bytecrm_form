import 'package:flutter/material.dart';

class FormFirstPage extends StatelessWidget {
  const FormFirstPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: false,
      ),
    );
  }
}
